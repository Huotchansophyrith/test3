$(document).ready(function () {
    document.cookie = "SALE=" + userPermiss['SALE'];
    document.cookie = "IMPORT=" + userPermiss['IMPORT'];
    document.cookie = "EXPORT=" + userPermiss['EXPORT'];
    document.cookie = "ACCOUNT=" + userPermiss['ACCOUNT'];
    document.cookie = "SETUP=" + userPermiss['SETUP'];
});